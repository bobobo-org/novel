# Rollback policy

A rollback must target the same model ID, match the current version's declared rollback target, and reference a previously approved, Production, or deprecated version. Rollback events are excluded from positive preference learning. Phase 0 validates targets only; no runtime model is changed.

