# Evaluator contract

Version: `closed-ai-evaluator-v1`

Evaluation supports deterministic rules, specialist evaluators, human review, and multi-evaluator aggregation. Records are traceable to a target, dataset version, training run, benchmark, scores, privacy failures, closed-only failures, and safety failures. Model self-rating alone is insufficient.

Phase 0 executes deterministic contract checks only.

