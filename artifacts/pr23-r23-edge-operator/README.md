# PR23 R2.3 Native Edge operator evidence

This directory is reserved for a fresh, operator-assisted Microsoft Edge run.
The tooling commit contains only this notice and the evidence schema. Runtime
records are created only after the native Local Network Access decision.

The immutable R2.2 evidence remains in `artifacts/pr23-r22-luna-unblock`.
