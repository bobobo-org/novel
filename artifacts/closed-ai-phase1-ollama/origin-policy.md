# Origin policy

Default allowlist: Production Studio, localhost:3000, and 127.0.0.1:3000. Preview origins require explicit HTTPS configuration. Wildcard CORS is forbidden. Browser writes require Origin, protocol header, JSON content type, bearer pairing token, and CSRF nonce. Host must be loopback.
