# Router integration

The existing Platform Router selected provider kind `local-ollama` for a real `closedOnly + device_only` request. A ready, preferred Gemini fixture was rejected by privacy policy. The Bridge-backed provider streamed a real candidate from `qwen2.5:3b`; external calls and data-left-device were both zero/false.
